CMAKE_MINIMUM_REQUIRED(VERSION 3.9.0)

PROJECT(test_pack)

SET(HEADER_FILES)

SET(SOURCE_FILES
    src/test_pack_main.cpp)

INCLUDE_DIRECTORIES($ENV{STD_PLUS_DIR}/include)

ADD_EXECUTABLE(test_pack ${SOURCE_FILES} ${HEADER_FILES})
